import streamlit as st
import difflib

# Устанавливаем настройки страницы (стильный темный/светлый дизайн по умолчанию)
st.set_page_config(page_title="Масса-Калькулятор КБЖУ", page_icon="💪", layout="centered")

# Культовая база данных для набора массы (БЖУ на 100 г или 100 мл)
FOOD_DATABASE = {
    # --- Источники белка ---
    "куриное филе (грудка)": {"калории": 113, "белки": 23.6, "жиры": 1.9, "углеводы": 0.4, "unit": "г"},
    "индейка (филе)": {"калории": 115, "белки": 21.6, "жиры": 2.2, "углеводы": 0.0, "unit": "г"},
    "говядина вырезка": {"калории": 158, "белки": 22.0, "жиры": 7.1, "углеводы": 0.0, "unit": "г"},
    "яйцо куриное целое (1 шт ≈ 50г)": {"калории": 157, "белки": 12.7, "жиры": 11.5, "углеводы": 0.7, "unit": "г"},
    "яичный белок жидкий": {"калории": 44, "белки": 11.1, "жиры": 0.2, "углеводы": 1.0, "unit": "г"},
    "творог 5%": {"калории": 121, "белки": 17.2, "жиры": 5.0, "углеводы": 1.8, "unit": "г"},
    "тунец в собственном соку": {"калории": 96, "белки": 21.0, "жиры": 1.2, "углеводы": 0.0, "unit": "г"},
    "протеин сывороточный (порошок)": {"калории": 390, "белки": 75.0, "жиры": 6.0, "углеводы": 8.0, "unit": "г"},
    
    # --- Сложные углеводы ---
    "гречневая крупа (сухая)": {"калории": 330, "белки": 12.6, "жиры": 3.3, "углеводы": 62.1, "unit": "г"},
    "рис бурый / жасмин (сухой)": {"калории": 340, "белки": 7.5, "жиры": 2.6, "углеводы": 71.0, "unit": "г"},
    "овсяные хлопья (геркулес)": {"калории": 352, "белки": 12.3, "жиры": 6.2, "углеводы": 61.8, "unit": "г"},
    "макароны из твердых сортов (сухие)": {"калории": 344, "белки": 12.0, "жиры": 1.5, "углеводы": 70.5, "unit": "г"},
    "банан": {"калории": 95, "белки": 1.5, "жиры": 0.2, "углеводы": 21.8, "unit": "г"},
    
    # --- Полезные жиры и ЖИДКОСТИ (в мл) ---
    "молоко 2.5%": {"калории": 52, "белки": 2.8, "жиры": 2.5, "углеводы": 4.7, "unit": "мл"},
    "молоко 3.2%": {"калории": 60, "белки": 2.9, "жиры": 3.2, "углеводы": 4.7, "unit": "мл"},
    "кефир 2.5%": {"калории": 50, "белки": 2.8, "жиры": 2.5, "углеводы": 4.0, "unit": "мл"},
    "арахисовая паста": {"калории": 588, "белки": 24.4, "жиры": 50.0, "углеводы": 20.0, "unit": "г"},
    "оливковое масло": {"калории": 898, "белки": 0.0, "жиры": 99.8, "углеводы": 0.0, "unit": "мл"},
    "орехи (миндаль / грецкий)": {"калории": 650, "белки": 18.0, "жиры": 55.0, "углеводы": 13.0, "unit": "г"},
}

# Инициализация сессии для хранения добавленных продуктов
if "meal_bag" not in st.session_state:
    st.session_state.meal_bag = []

st.title("💪 Калькулятор «Чистого Набора Массы»")
st.write("Сайт-калькулятор анаболической корзины. Ищите культовые продукты и собирайте свой идеальный рацион.")

# --- Блок интерфейса: Умный поиск ---
st.subheader("🔍 Поиск и добавление продуктов")

# Текстовое поле для ввода ключевого слова
search_input = st.text_input("Введите ключевое слово (например: курица, молоко, рис, прот):").lower().strip()

# Фильтрация базы данных по ключевому слову
if search_input:
    # Прямые совпадения
    filtered_foods = [food for food in FOOD_DATABASE.keys() if search_input in food.lower()]
    # Если нет прямых, используем нечеткий поиск
    if not filtered_foods:
        filtered_foods = difflib.get_close_matches(search_input, FOOD_DATABASE.keys(), n=5, cutoff=0.3)
else:
    filtered_foods = list(FOOD_DATABASE.keys())

# Выпадающий список с отфильтрованными продуктами
if filtered_foods:
    selected_product = st.selectbox("Выберите продукт из найденных:", filtered_foods)
    
    # Динамически меняем текст (г или мл) в зависимости от выбранного продукта
    unit = FOOD_DATABASE[selected_product]["unit"]
    
    # Ввод объема / веса
    amount = st.number_input(f"Укажите количество ({unit}):", min_value=1, max_value=2000, value=100, step=10)
    
    # Кнопка добавления в рацион
    if st.button("Добавить в тарелку ➕", use_container_width=True):
        nutrients = FOOD_DATABASE[selected_product]
        ratio = amount / 100.0
        
        # Добавляем позицию
        st.session_state.meal_bag.append({
            "name": selected_product,
            "amount": amount,
            "unit": unit,
            "kcal": nutrients["калории"] * ratio,
            "p": nutrients["белки"] * ratio,
            "f": nutrients["жиры"] * ratio,
            "c": nutrients["углеводы"] * ratio
        })
        st.success(f"Добавлено: {selected_product} ({amount} {unit})")
else:
    st.error("Ничего не найдено. Попробуйте ввести другое слово (например, 'овсян', 'молок').")

st.markdown("---")

# --- Блок интерфейса: Ваша Анаболическая Тарелка ---
st.subheader("📥 Ваша текущая корзина питания")

if st.session_state.meal_bag:
    total_kcal = total_p = total_f = total_c = 0
    
    # Отображаем список красивыми строками
    for idx, item in enumerate(st.session_state.meal_bag):
        total_kcal += item["kcal"]
        total_p += item["p"]
        total_f += item["f"]
        total_c += item["c"]
        
        st.write(f"▪️ **{item['name']}** — {item['amount']} {item['unit']} | "
                 f"🔥 {round(item['kcal'])} ккал | Б: {round(item['p'], 1)}г | Ж: {round(item['f'], 1)}г | У: {round(item['c'], 1)}г")
    
    st.markdown("---")
    
    # Итоговые карточки (Метрики)
    st.markdown("### 📊 ИТОГО ЗА ПРИЕМ ПИЩИ:")
    col1, col2, col3, col4 = st.columns(4)
    
    with col1:
        st.metric(label="Калории", value=f"{round(total_kcal)} ккал")
    with col2:
        st.metric(label="Белки (🍗)", value=f"{round(total_p, 1)} г")
    with col3:
        st.metric(label="Жиры (🥑)", value=f"{round(total_f, 1)} г")
    with col4:
        st.metric(label="Углеводы (🍞)", value=f"{round(total_c, 1)} г")
        
    # Кнопка сброса
    if st.button("Очистить корзину 🗑️", type="secondary"):
        st.session_state.meal_bag = []
        st.rerun()
else:
    st.info("Корзина пуста. Добавьте продукты выше, чтобы рассчитать общую сумму анаболической массы.")